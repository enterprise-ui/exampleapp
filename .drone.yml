kind: pipeline
name: default

steps:
  - name: build
    image: node:current-alpine
    environment:
      NPMRC:
        from_secret: NPMRC   
    commands:   
      - npm config set $NPMRC
      - yarn
      - cd packages/exampleapp-api
      - yarn build

